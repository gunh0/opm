{
  "name": "back",
  "version": "1.0.0",
  "description": "back-server",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --watch src --exec \"npx\" \"ts-node\" src/app.js",
    "clean": "rm -rf .next out .turbo"
  },
  "devDependencies": {
    "@types/node": "^17.0.33",
    "eslint-config-custom": "*",
    "tsconfig": "*"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  }
}
